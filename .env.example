# Public client config — only VITE_* vars are exposed to the browser.
# Copy this file to `.env.local` and fill in values.

VITE_APP_NAME="Aura AI"
VITE_API_BASE_URL="http://localhost:8080"

# Optional
VITE_SENTRY_DSN=""
VITE_ANALYTICS_KEY=""
